
  # SESC

  This is a code bundle for SESC. The original project is available at https://www.figma.com/design/dbWpQp1UMYdhjTHTU4hh0b/SESC.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  